<%= form_with(model: acceuil) do |form| %>
  <% if acceuil.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(acceuil.errors.count, "error") %> prohibited this acceuil from being saved:</h2>

      <ul>
        <% acceuil.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_area :text %>
  </div>

  <div class="field">
    <%= form.label :photo %>
    <%= form.text_area :photo %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author %>
  </div>

  <div class="field">
    <%= form.label :published_at %>
    <%= form.datetime_select :published_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
